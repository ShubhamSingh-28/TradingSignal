
services:
  postgres:
    image: postgres:16-alpine
    container_name: trading-signal-db
    environment:
      POSTGRES_DB: trading_signal_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: